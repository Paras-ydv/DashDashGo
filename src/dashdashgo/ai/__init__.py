"""Optional AI assistant: failure diagnosis and config drafting.

Works with any OpenAI-compatible chat API (Gemini, Groq, OpenRouter, OpenAI,
a local Ollama). Everything the pipeline does works without it; with no
``AI_API_KEY`` the features are hidden in the UI and refused by the API/CLI.
"""
